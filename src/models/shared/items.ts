export interface Item {
  id: string;            // UUID único do item
  description: string;      // Nome ou descrição do material
  quantity: number;      // Quantidade
  value: number;     // Preço unitário
}
